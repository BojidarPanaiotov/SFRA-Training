<isscript>
    var assets = require('*/cartridge/scripts/assets.js');
    assets.addCss('/css/customCategory.css');
</isscript>

<div class="hero-section" style="">
    <div class="hero-section-text" style="${pdict.category_position}">
        <a id="hero-link" href="${URLUtils.url('Search-Show','cgid',pdict.category_id)}"
            class="cat_title">${pdict.category_title}</a>
        <p>${pdict.category_desc}</p>
    </div>
</div>
