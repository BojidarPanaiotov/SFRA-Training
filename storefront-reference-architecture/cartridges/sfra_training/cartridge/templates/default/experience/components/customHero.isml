<isscript>
    var assets = require('*/cartridge/scripts/assets.js');
    assets.addCss('/css/customHero.css');
</isscript>

<div class="hero_container" style="background-image: url('${pdict.bg_image}');">
    <h1 id="hero_title">${pdict.title}</h1>
    <p id="hero_short_desc">${pdict.short_desc}</p>
    <a id="hero_cta" href="${'https://demo-disney-ecommera.demandware.net'+URLUtils.url('Product-Show','pid',pdict.product.ID)}">${pdict.cta}</a>
</div>
